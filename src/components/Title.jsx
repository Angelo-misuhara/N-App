

const Title = ({title}) => {
  return (
      <div className="flex justify-center p-3 bg-stone-100 flex-wrap">
        <h1 className="font-bold text-7xl" id="title">{title}</h1>
      </div>
  )
}

export default Title